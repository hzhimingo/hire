server:
  port: ${PORT:7001}
  tomcat:
    uri-encoding: UTF-8
    max-swallow-size: -1
spring:
  application:
    name: hire-user-service
  profiles:
    active: dev
mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml
